class Recipe {
  String title;
  String user;
  String imageurl;
  String description;
  bool isFavorite;
  int favoriteCount;

  Recipe(this.title, this.user, this.imageurl, this.description,
      this.isFavorite, this.favoriteCount);
}
